# 144. Binary Tree Preorder Traversal

🔗 LeetCode Problem: https://leetcode.com/problems/binary-tree-preorder-traversal/

---

## 📌 Problem Summary
Return the preorder traversal of a binary tree.

Preorder follows the order:

Node → Left → Right

---

## 🧠 Core Concept

Preorder visits the root before its subtrees.

Two ways to implement:
- Recursive (DFS)
- Iterative using Stack (optimal & interview preferred)

---

# ✅ Optimal Approach — Iterative (Stack)

### 🎯 Idea:
Simulate recursion manually.

Visit node first, then explore left subtree, and later right subtree using stack.

---

## 📋 Algorithm

1. Initialize stack and set `curr = root`
2. While `curr != null` or stack not empty:
   - While `curr != null`:
     - Add value to result
     - Push node to stack
     - Move to left child
   - Pop from stack
   - Move to right child
3. Repeat until traversal finishes

---

## ⏱ Complexity

Time: O(n)  
Space: O(h) where h = tree height

---

## 💻 Java Implementation (Iterative)

```java
class Solution {
    public List<Integer> preorderTraversal(TreeNode root) {
        List<Integer> result = new ArrayList<>();
        Stack<TreeNode> stack = new Stack<>();
        TreeNode curr = root;

        while (curr != null || !stack.isEmpty()) {
            while (curr != null) {
                result.add(curr.val);
                stack.push(curr);
                curr = curr.left;
            }

            curr = stack.pop();
            curr = curr.right;
        }

        return result;
    }
}

RECURSIVE

class Solution {
    public List<Integer> preorderTraversal(TreeNode root) {
        List<Integer> result = new ArrayList<>();
        preorder(root, result);
        return result;
    }

    private void preorder(TreeNode node, List<Integer> result) {
        if (node == null) return;

        result.add(node.val);
        preorder(node.left, result);
        preorder(node.right, result);
    }
}
